# Server port
PORT=4000

# Mongo Atlas connection string
MONGO_URL="mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/yqn_pos?retryWrites=true&w=majority"

# Optional: fail faster in local dev if Atlas/network is unreachable (default 5000)
# MONGO_SERVER_SELECTION_TIMEOUT_MS=5000

# Used to generate customerUrl for QR codes
BASE_URL=http://localhost:4000
